<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraint_layout_add_med_times"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".AddMedTimesFragment">

    <include layout="@layout/add_med_toolbar"/>

    <include
        android:id="@+id/include7"
        layout="@layout/add_med_header"
        tools:layout_editor_absoluteY="56dp" />

    <include layout="@layout/add_med_next_button"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_med_times_add_med"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="96dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/include7" />

</androidx.constraintlayout.widget.ConstraintLayout>